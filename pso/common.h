#ifndef __COMMON_H
#define __COMMON_H

#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) > (b) ? (a) : (b))

typedef int int_t;
typedef double flt_t;
typedef char *str_t;

flt_t flt_rand(void);

#endif
